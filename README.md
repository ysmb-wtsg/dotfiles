# detfiles
this repository contains not only dotfiles, but also the items listed in `etc` directory.
- VSCode
  - extensions
  - settings
  - keybindings
- brew packages
- starship config
- vimium option

## Usage
### dump current settings
```sh
$ sh dump.sh
```

### install current settings
```sh
$ sh install.sh
```
